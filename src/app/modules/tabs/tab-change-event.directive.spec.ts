import { TabChangeEventDirective } from './tab-change-event.directive';

describe('TabChangeEventDirective', () => {
  it('should create an instance', () => {
    const directive = new TabChangeEventDirective();
    expect(directive).toBeTruthy();
  });
});
