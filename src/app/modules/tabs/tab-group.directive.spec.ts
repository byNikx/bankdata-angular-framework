import { TabGroupDirective } from './tab-group.directive';

describe('TabGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new TabGroupDirective();
    expect(directive).toBeTruthy();
  });
});
