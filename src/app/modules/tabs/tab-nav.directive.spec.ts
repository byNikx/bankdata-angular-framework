import { TabNavDirective } from './tab-nav.directive';

describe('TabNavDirective', () => {
  it('should create an instance', () => {
    const directive = new TabNavDirective();
    expect(directive).toBeTruthy();
  });
});
